<%= erb :_header %>

<div class="container">
  <CAPTION><EM><h1>Categories</h1></EM></CAPTION>
  <% @category.each do |elem| %>
  <%= "<a href = /#{elem.id}>" %>
    <p>
     <%= elem.name %>
    </p>
  </a>
  <% end %>
</div>